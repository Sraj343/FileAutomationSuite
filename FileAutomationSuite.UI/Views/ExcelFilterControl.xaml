<Page x:Class="FileAutomationSuite.UI.Views.ExcelFilterControl"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d"
      d:DesignHeight="600"
      d:DesignWidth="800"
      Title="Excel Filter Control">

    <!-- ================= RESOURCES ================= -->
    <Page.Resources>

        <!-- Button Style -->
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Height" Value="34"/>
            <Setter Property="Padding" Value="14,0"/>
            <Setter Property="Background" Value="#2E8B57"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="6">
                            <ContentPresenter
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- TextBox Style -->
        <Style x:Key="InputTextBox" TargetType="TextBox">
            <Setter Property="Height" Value="30"/>
            <Setter Property="Padding" Value="6"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

    </Page.Resources>

    <!-- ================= LAYOUT ================= -->
    <Grid Background="#F5F6F8">
        <Grid Margin="40"
              HorizontalAlignment="Center"
              Width="650">

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <!-- Header -->
                <RowDefinition Height="Auto"/>
                <!-- Form -->
                <RowDefinition Height="Auto"/>
                <!-- Submit -->
                <RowDefinition Height="20"/>
                <!-- Progress space -->
            </Grid.RowDefinitions>

            <!-- ================= HEADER ================= -->
            <TextBlock Text="Excel Filter Control"
                       FontSize="24"
                       FontWeight="Bold"
                       Margin="0,0,0,25"
                       HorizontalAlignment="Center"/>

            <!-- ================= FORM ================= -->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="220"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Upload Excel -->
                <TextBlock Text="Upload Excel File:"
                           Grid.Row="0"
                           Grid.Column="0"
                           VerticalAlignment="Center"
                           Margin="0,0,10,12"/>

                <DockPanel Grid.Row="0" Grid.Column="1" Margin="0,0,0,12">
                    <Button Content="Browse"
                            Width="110"
                            Style="{StaticResource PrimaryButton}"
                            Click="UploadExcel_Click"
                            Margin="0,0,10,0"/>
                    <TextBox x:Name="txtInputPath"
                             Style="{StaticResource InputTextBox}"
                             IsReadOnly="True"/>
                </DockPanel>

                <!-- Output Path -->
                <TextBlock Text="Output Folder:"
                           Grid.Row="1"
                           Grid.Column="0"
                           VerticalAlignment="Center"
                           Margin="0,0,10,12"/>

                <DockPanel Grid.Row="1" Grid.Column="1" Margin="0,0,0,12">
                    <Button Content="Browse"
                            Width="110"
                            Style="{StaticResource PrimaryButton}"
                            Click="SelectOutputPath_Click"
                            Margin="0,0,10,0"/>
                    <TextBox x:Name="txtOutputPath"
                             Style="{StaticResource InputTextBox}"
                             IsReadOnly="True"/>
                </DockPanel>

                <!-- Filter Column -->
                <TextBlock Text="Filter Column:"
                           Grid.Row="2"
                           Grid.Column="0"
                           VerticalAlignment="Center"
                           Margin="0,0,10,12"/>

                <TextBox x:Name="txtFilterColumn"
                         Grid.Row="2"
                         Grid.Column="1"
                         Style="{StaticResource InputTextBox}"
                         Margin="0,0,0,12"/>

                <!-- Select Columns -->
                <GroupBox Header="Select Columns"
                          Grid.Row="3"
                          Grid.Column="0"
                          Grid.ColumnSpan="2"
                          Margin="0,0,0,15">

                    <StackPanel Margin="10">
                        <CheckBox x:Name="chkSelectAllColumns"
                                  Content="Select All Columns"
                                  FontWeight="SemiBold"
                                  Margin="0,0,0,8"
                                  Checked="ChkSelectAll_Checked"
                                  Unchecked="ChkSelectAll_Unchecked"/>

                        <Border BorderBrush="#DDDDDD"
                                BorderThickness="1"
                                CornerRadius="6">
                            <ScrollViewer Height="160">
                                <ListBox x:Name="lstColumns"
                                         SelectionMode="Multiple"
                                         BorderThickness="0"
                                         SelectionChanged="LstColumns_SelectionChanged">
                                    <ListBox.ItemTemplate>
                                        <DataTemplate>
                                            <CheckBox Content="{Binding}"
                                                      IsChecked="{Binding RelativeSource={RelativeSource AncestorType=ListBoxItem},
                                                                          Path=IsSelected,
                                                                          Mode=TwoWay}"
                                                      Margin="5"/>
                                        </DataTemplate>
                                    </ListBox.ItemTemplate>
                                </ListBox>
                            </ScrollViewer>
                        </Border>
                    </StackPanel>
                </GroupBox>

                <!-- Options -->
                <CheckBox x:Name="chkIsOriginal"
                          Grid.Row="4"
                          Grid.Column="1"
                          Content="Include original sheet in output"
                          Margin="0,0,0,10"/>
            </Grid>

            <!-- ================= SUBMIT ================= -->
            <Button Grid.Row="2"
                    Content="Submit"
                    Width="180"
                    Height="40"
                    Style="{StaticResource PrimaryButton}"
                    HorizontalAlignment="Center"
                    Click="Submit_Click"/>

            <!-- ================= PROGRESS ================= -->
            <ProgressBar x:Name="progressBar"
                         Grid.Row="3"
                         Height="6"
                         Visibility="Collapsed"
                         IsIndeterminate="True"/>
        </Grid>
    </Grid>
</Page>
